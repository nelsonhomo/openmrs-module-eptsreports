package org.openmrs.module.eptsreports.reporting.library.queries.data.quality;

public class EC26Queries {

  public static String getEc26CombinedQuery() {
    String query =
        "SELECT "
            + " pe.person_id As patient_id, "
            + " pid.identifier AS NID, "
            + " concat(ifnull(pn.given_name,''),' ',ifnull(pn.middle_name,''),' ',ifnull(pn.family_name,'')) AS Name, "
            + " DATE_FORMAT(pe.birthdate, '%d-%m-%Y') AS birthdate, "
            + " IF(pe.birthdate_estimated = 1, 'Sim','Não') AS Estimated_dob, "
            + " pe.gender AS Sex, DATE_FORMAT(pe.date_created, '%d-%m-%Y') AS First_entry_date, "
            + " DATE_FORMAT(pe.date_changed, '%d-%m-%Y') AS Last_updated, "
            + " DATE_FORMAT(programState.date_enrolled, '%d-%m-%Y') AS date_enrolled, "
            + " case "
            + " when programState.state = 9 then 'DROPPED FROM TREATMENT' "
            + " when programState.state = 6 then 'ACTIVE ON PROGRAM' "
            + " when programState.state = 10 then 'PATIENT HAS DIED' "
            + " when programState.state = 8 then 'SUSPENDED TREATMENT' "
            + " when programState.state = 7 then 'TRANSFERED OUT TO ANOTHER FACILITY' "
            + " when programState.state = 29 then 'TRANSFERRED FROM OTHER FACILTY' "
            + " end AS state, "
            + " DATE_FORMAT(programState.start_date, '%d-%m-%Y') AS state_date, "
            + " l.name AS location_name, "
            + " DATE_FORMAT(fichaClinica.encounter_datetime, '%d-%m-%Y') AS dataDaFichaClinica, "
            + "  DATE_FORMAT(fichaClinica.date_created, '%d-%m-%Y') AS dataDaCriacao, "
            + "   case "
            + " when fichaClinica.value_coded = 656 then 'INH' "
            + " when fichaClinica.value_coded = 23954 then '3HP' "
            + " when fichaClinica.value_coded = 165305 then '1HP' "
            + " when fichaClinica.value_coded = 165306 then 'LFX' "
            + " end AS value_coded "
            + " FROM "
            + " person pe "
            + " INNER JOIN "
            + " ( select pn1.* "
            + " from person_name pn1 "
            + " inner join "
            + " ( "
            + " select person_id, min(person_name_id) id "
            + " from person_name "
            + " where voided = 0 "
            + " group by person_id "
            + " ) pn2 "
            + " where pn1.person_id = pn2.person_id and pn1.person_name_id = pn2.id "
            + " ) pn on pn.person_id = pe.person_id "
            + " INNER JOIN "
            + " ( select pid1.* "
            + " from patient_identifier pid1 "
            + " inner join "
            + " ( "
            + " select patient_id, min(patient_identifier_id) id "
            + " from patient_identifier "
            + " where voided = 0 "
            + " group by patient_id "
            + " ) pid2 "
            + " where pid1.patient_id = pid2.patient_id and pid1.patient_identifier_id = pid2.id "
            + " ) pid on pid.patient_id = pe.person_id "
            + "  INNER JOIN location l ON l.location_id = pid.location_id "
            + " LEFT JOIN (SELECT pg.patient_id, pg.date_enrolled, ps.state, max(ps.start_date) AS start_date "
            + " FROM patient_program pg "
            + " INNER JOIN patient_state ps ON pg.patient_program_id = ps.patient_program_id "
            + " AND ps.start_date IS NOT NULL "
            + " AND ps.end_date IS NULL "
            + " AND pg.program_id = 2 "
            + " AND pg.location_id =:location "
            + " GROUP BY pg.patient_id "
            + " ) AS programState ON pe.person_id = programState.patient_id "
            + " inner join ( "
            + " SELECT regime.patient_id,regime.encounter_id, regime.encounter_datetime,regime.date_created, regime.value_coded "
            + " FROM (SELECT  p.patient_id, e.encounter_id,e.encounter_datetime,e.date_created,obsRegime.value_coded FROM  patient p "
            + "INNER JOIN encounter e on p.patient_id = e.patient_id "
            + "inner join obs obsRegime on e.encounter_id = obsRegime.encounter_id "
            + "where  p.voided = 0 and e.voided = 0 and obsRegime.voided = 0 "
            + "and e.encounter_type = 6 "
            + "AND obsRegime.concept_id = 23985 and obsRegime.value_coded is not null and e.location_id =:location "
            + ") regime "
            + "left join "
            + "( "
            + "SELECT  p.patient_id, e.encounter_id FROM  patient p "
            + "INNER JOIN encounter e on p.patient_id = e.patient_id "
            + "inner join obs obsEstado on e.encounter_id = obsEstado.encounter_id "
            + "where  p.voided = 0 and e.voided = 0 and obsEstado.voided = 0 "
            + "and e.encounter_type = 6 "
            + "AND obsEstado.concept_id = 165308 and obsEstado.value_coded is not null and e.location_id =:location "
            + ") estado on estado.patient_id = regime.patient_id "
            + "and estado.encounter_id = regime.encounter_id where estado.patient_id  is null "
            + "order by regime.patient_id "
            + " ) fichaClinica on fichaClinica.patient_id = pe.person_id ";
    return query;
  }

  public static String getEc26Total() {
    String query =
        "SELECT "
            + " pe.person_id As patient_id "
            + " FROM "
            + " person pe "
            + " INNER JOIN "
            + " ( select pn1.* "
            + " from person_name pn1 "
            + " inner join "
            + " ( "
            + " select person_id, min(person_name_id) id "
            + " from person_name "
            + " where voided = 0 "
            + " group by person_id "
            + " ) pn2 "
            + " where pn1.person_id = pn2.person_id and pn1.person_name_id = pn2.id "
            + " ) pn on pn.person_id = pe.person_id "
            + " INNER JOIN "
            + " ( select pid1.* "
            + " from patient_identifier pid1 "
            + " inner join "
            + " ( "
            + " select patient_id, min(patient_identifier_id) id "
            + " from patient_identifier "
            + " where voided = 0 "
            + " group by patient_id "
            + " ) pid2 "
            + " where pid1.patient_id = pid2.patient_id and pid1.patient_identifier_id = pid2.id "
            + " ) pid on pid.patient_id = pe.person_id "
            + "  INNER JOIN location l ON l.location_id = pid.location_id "
            + " LEFT JOIN (SELECT pg.patient_id, pg.date_enrolled, ps.state, max(ps.start_date) AS start_date "
            + " FROM patient_program pg "
            + " INNER JOIN patient_state ps ON pg.patient_program_id = ps.patient_program_id "
            + " AND ps.start_date IS NOT NULL "
            + " AND ps.end_date IS NULL "
            + " AND pg.program_id = 2 "
            + " AND pg.location_id =:location "
            + " GROUP BY pg.patient_id "
            + " ) AS programState ON pe.person_id = programState.patient_id "
            + " inner join ( "
            + " SELECT regime.patient_id,regime.encounter_id, regime.encounter_datetime FROM (SELECT  p.patient_id, e.encounter_id,e.encounter_datetime FROM  patient p "
            + "INNER JOIN encounter e on p.patient_id = e.patient_id "
            + "inner join obs obsRegime on e.encounter_id = obsRegime.encounter_id "
            + "where  p.voided = 0 and e.voided = 0 and obsRegime.voided = 0 "
            + "and e.encounter_type = 6 "
            + "AND obsRegime.concept_id = 23985 and obsRegime.value_coded is not null and e.location_id =:location "
            + ") regime "
            + "left join "
            + "( "
            + "SELECT  p.patient_id, e.encounter_id FROM  patient p "
            + "INNER JOIN encounter e on p.patient_id = e.patient_id "
            + "inner join obs obsEstado on e.encounter_id = obsEstado.encounter_id "
            + "where  p.voided = 0 and e.voided = 0 and obsEstado.voided = 0 "
            + "and e.encounter_type = 6 "
            + "AND obsEstado.concept_id = 165308 and obsEstado.value_coded is not null and e.location_id =:location "
            + ") estado on estado.patient_id = regime.patient_id "
            + "and estado.encounter_id = regime.encounter_id where estado.patient_id  is null "
            + "order by regime.patient_id "
            + " ) fichaClinica on fichaClinica.patient_id = pe.person_id "
            + " group by  pe.person_id ";
    return query;
  }
}
